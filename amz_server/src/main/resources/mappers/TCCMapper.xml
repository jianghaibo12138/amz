<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.amazing2j.amz.amz_server.mapper.TCCMapper">

    <select id="getLatestTCCTxId" resultType="java.lang.Long">
        SELECT DISTINCT tx_id
        FROM amz_TCC
        ORDER BY create_at
        limit 1;
    </select>

    <select id="getTCCEntityForUpdateByTxId" resultType="com.amazing2j.amz.amz_server.model.entity.TCCEntity"
            parameterType="long">
        SELECT *
        FROM amz_TCC
        WHERE tx_id = #{txId}
            FOR
        UPDATE;
    </select>

    <update id="updateTxBranchStatus">
        UPDATE amz_TCC
        SET branch_status = #{branchStatus}
        WHERE id = #{branchId}
    </update>

    <update id="updateTxExecutor">
        UPDATE amz_TCC
        SET executor = #{executor}
        WHERE id = #{branchId}
    </update>
</mapper>